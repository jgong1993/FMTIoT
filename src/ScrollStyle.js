import { StyleSheet } from 'react-native';

var ScrollStyle = StyleSheet.create({

    container: {
        flex: .1
    },

    numbers: {
        fontSize: 25
    },

});

export default ScrollStyle;